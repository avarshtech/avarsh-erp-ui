/**
 * Bill Passing mock switch.
 *
 * Flip to `false` once /api/v1/inventory/bill-passing exists and
 * billPassingApi.js has been written — the screens never read this flag, so the
 * cutover is a one-line change per function in billPassingService.js.
 */
export const USE_MOCK_BILL_PASSING_DATA = true;
