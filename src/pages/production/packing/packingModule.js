// RBAC module id for the carton packing screens, kept in its own module so the
// list and the form — separate lazy route chunks — can share it without either
// importing the other. Mirrors ProductionMasterPanel's exported MODULE_ID.
export const MODULE_ID = 'production-packing';
